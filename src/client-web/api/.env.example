# Database Configuration
# Copy this file to .env and update with your database credentials
# The .env file can be placed in any of these locations:
#   - src/client-web/api/.env (local)
#   - src/.env (if src/ downloaded standalone)
#   - Project root/.env (if full project deployed)

DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=transit_system
